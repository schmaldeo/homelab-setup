---
docker_user: docker
docker_workdir: "/home/docker/dimgs"
docker_user_password_hash: "{{ docker_user_pw }}"
docker_debian_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
docker_fedora_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
